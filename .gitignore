# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Node (defensive — project is zero-deps, but if anyone runs npm later)
node_modules/
package-lock.json

# Local dev artifacts
_light-wrap*.html
/tmp/

# Phase screenshot outputs (large binary, regenerable)
scratch/phase*-screenshots/
scratch/phase*-final/

# Local secrets (none today, but in case)
.env
.env.local

# ATC repair sweep caches (regenerable RxNav lookups, not source data)
scratch/atc-repair/.enrich-cache.json
